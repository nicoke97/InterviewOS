id: SD91
level: SD
page: 91
set: 10
block: B
block_id: SD.B
order: 1
scaffolding: full
time_estimate_seconds: 66
prompt: Combina {"a":1} y {"b":2} en uno nuevo; imprime cuántas llaves tiene (2).
hints:
- Copia el primero y luego agrega/actualiza con el segundo.
validation:
  type: run_and_match_stdout
  expected: '2'
starter_code: 'var a = ___;

  var b = ___;

  var c = ___;

  foreach (var kv in b) c[kv.Key] = ___;

  Console.WriteLine(___);'
reference_code: 'var a = new Dictionary<string, int> { ["a"] = 1 };

  var b = new Dictionary<string, int> { ["b"] = 2 };

  var c = new Dictionary<string, int>(a);

  foreach (var kv in b) c[kv.Key] = kv.Value;

  Console.WriteLine(c.Count);'
