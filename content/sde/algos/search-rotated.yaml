id: search-rotated
title: Search in Rotated Sorted Array
leetcode_id: lc-search-rotated
test_cases:
- args:
  - - 4
    - 5
    - 6
    - 7
    - 0
    - 1
    - 2
  - 0
  expected: 4
- args:
  - - 4
    - 5
    - 6
    - 7
    - 0
    - 1
    - 2
  - 3
  expected: -1
voice:
  prompt: ¿Por qué O(log n) y cuándo no aplica?
  prompt_en: Explain the approach and the complexity out loud.
  keywords:
  - o(n)
  - o(1)
  - complejidad
  - enfoque
  - invariante
  - respuesta
  - diferencia
  - buscar
  - valor
  - exacto
languages:
  csharp:
    fn_name: SearchRotated
    sheets:
    - id: r1-01
      rung: 1
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int SearchRotated(int[] nums, int target)\n{\n    ___\n    while (lo <= hi)\n   \
        \ {\n        int mid = lo + (hi - lo) / 2;\n        if (nums[mid] == target) return mid;\n   \
        \     if (nums[lo] <= nums[mid])\n        {\n            if (nums[lo] <= target && target < nums[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (nums[mid] < target && target <= nums[hi]) lo = mid + 1;\n            else hi = mid -\
        \ 1;\n        }\n    }\n    return -1;\n}\n"
    - id: r1-02
      rung: 1
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int SearchRotated(int[] arr, int goal)\n{\n    ___\n    while (lo <= hi)\n    {\n\
        \        int mid = lo + (hi - lo) / 2;\n        if (arr[mid] == goal) return mid;\n        if\
        \ (arr[lo] <= arr[mid])\n        {\n            if (arr[lo] <= goal && goal < arr[mid]) hi = mid\
        \ - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n            if (arr[mid]\
        \ < goal && goal <= arr[hi]) lo = mid + 1;\n            else hi = mid - 1;\n        }\n    }\n\
        \    return -1;\n}\n"
    - id: r1-03
      rung: 1
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int SearchRotated(int[] xs, int want)\n{\n    ___\n    while (lo <= hi)\n    {\n\
        \        int mid = lo + (hi - lo) / 2;\n        if (xs[mid] == want) return mid;\n        if (xs[lo]\
        \ <= xs[mid])\n        {\n            if (xs[lo] <= want && want < xs[mid]) hi = mid - 1;\n  \
        \          else lo = mid + 1;\n        }\n        else\n        {\n            if (xs[mid] < want\
        \ && want <= xs[hi]) lo = mid + 1;\n            else hi = mid - 1;\n        }\n    }\n    return\
        \ -1;\n}\n"
    - id: r1-04
      rung: 1
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int SearchRotated(int[] values, int cap)\n{\n    ___\n    while (lo <= hi)\n    {\n\
        \        int mid = lo + (hi - lo) / 2;\n        if (values[mid] == cap) return mid;\n        if\
        \ (values[lo] <= values[mid])\n        {\n            if (values[lo] <= cap && cap < values[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (values[mid] < cap && cap <= values[hi]) lo = mid + 1;\n            else hi = mid - 1;\n\
        \        }\n    }\n    return -1;\n}\n"
    - id: r1-05
      rung: 1
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int SearchRotated(int[] items, int limit)\n{\n    ___\n    while (lo <= hi)\n   \
        \ {\n        int mid = lo + (hi - lo) / 2;\n        if (items[mid] == limit) return mid;\n   \
        \     if (items[lo] <= items[mid])\n        {\n            if (items[lo] <= limit && limit < items[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (items[mid] < limit && limit <= items[hi]) lo = mid + 1;\n            else hi = mid -\
        \ 1;\n        }\n    }\n    return -1;\n}\n"
    - id: r2-01
      rung: 2
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int SearchRotated(int[] nums, int target)\n{\n    int lo = 0, hi = nums.Length -\
        \ 1;\n    ___\n        }\n    }\n    return -1;\n}\n"
    - id: r2-02
      rung: 2
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int SearchRotated(int[] arr, int goal)\n{\n    int lo = 0, hi = arr.Length - 1;\n\
        \    ___\n        }\n    }\n    return -1;\n}\n"
    - id: r2-03
      rung: 2
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int SearchRotated(int[] xs, int want)\n{\n    int lo = 0, hi = xs.Length - 1;\n \
        \   ___\n        }\n    }\n    return -1;\n}\n"
    - id: r2-04
      rung: 2
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int SearchRotated(int[] values, int cap)\n{\n    int lo = 0, hi = values.Length -\
        \ 1;\n    ___\n        }\n    }\n    return -1;\n}\n"
    - id: r2-05
      rung: 2
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int SearchRotated(int[] items, int limit)\n{\n    int lo = 0, hi = items.Length -\
        \ 1;\n    ___\n        }\n    }\n    return -1;\n}\n"
    - id: r3-01
      rung: 3
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int SearchRotated(int[] nums, int target)\n{\n    int lo = 0, hi = nums.Length -\
        \ 1;\n    while (lo <= hi)\n    {\n        int mid = lo + (hi - lo) / 2;\n        ___\n      \
        \  if (nums[lo] <= nums[mid])\n        {\n            if (nums[lo] <= target && target < nums[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (nums[mid] < target && target <= nums[hi]) lo = mid + 1;\n            else hi = mid -\
        \ 1;\n        }\n    }\n    ___\n}\n"
    - id: r3-02
      rung: 3
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int SearchRotated(int[] arr, int goal)\n{\n    int lo = 0, hi = arr.Length - 1;\n\
        \    while (lo <= hi)\n    {\n        int mid = lo + (hi - lo) / 2;\n        ___\n        if (arr[lo]\
        \ <= arr[mid])\n        {\n            if (arr[lo] <= goal && goal < arr[mid]) hi = mid - 1;\n\
        \            else lo = mid + 1;\n        }\n        else\n        {\n            if (arr[mid]\
        \ < goal && goal <= arr[hi]) lo = mid + 1;\n            else hi = mid - 1;\n        }\n    }\n\
        \    ___\n}\n"
    - id: r3-03
      rung: 3
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int SearchRotated(int[] xs, int want)\n{\n    int lo = 0, hi = xs.Length - 1;\n \
        \   while (lo <= hi)\n    {\n        int mid = lo + (hi - lo) / 2;\n        ___\n        if (xs[lo]\
        \ <= xs[mid])\n        {\n            if (xs[lo] <= want && want < xs[mid]) hi = mid - 1;\n  \
        \          else lo = mid + 1;\n        }\n        else\n        {\n            if (xs[mid] < want\
        \ && want <= xs[hi]) lo = mid + 1;\n            else hi = mid - 1;\n        }\n    }\n    ___\n\
        }\n"
    - id: r3-04
      rung: 3
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int SearchRotated(int[] values, int cap)\n{\n    int lo = 0, hi = values.Length -\
        \ 1;\n    while (lo <= hi)\n    {\n        int mid = lo + (hi - lo) / 2;\n        ___\n      \
        \  if (values[lo] <= values[mid])\n        {\n            if (values[lo] <= cap && cap < values[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (values[mid] < cap && cap <= values[hi]) lo = mid + 1;\n            else hi = mid - 1;\n\
        \        }\n    }\n    ___\n}\n"
    - id: r3-05
      rung: 3
      kind: rung
      fn_name: SearchRotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int SearchRotated(int[] items, int limit)\n{\n    int lo = 0, hi = items.Length -\
        \ 1;\n    while (lo <= hi)\n    {\n        int mid = lo + (hi - lo) / 2;\n        ___\n      \
        \  if (items[lo] <= items[mid])\n        {\n            if (items[lo] <= limit && limit < items[mid])\
        \ hi = mid - 1;\n            else lo = mid + 1;\n        }\n        else\n        {\n        \
        \    if (items[mid] < limit && limit <= items[hi]) lo = mid + 1;\n            else hi = mid -\
        \ 1;\n        }\n    }\n    ___\n}\n"
    - id: full-01
      rung: 4
      kind: full
      fn_name: SearchRotated
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "int SearchRotated(int[] nums, int target)\n{\n    return -1;\n}\n"
    - id: full-02
      rung: 4
      kind: full
      fn_name: SearchRotated
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "int SearchRotated(int[] arr, int goal)\n{\n    return -1;\n}\n"
    - id: full-recall
      rung: 4
      kind: full
      fn_name: SearchRotated
      prompt: 'Day 2 / pool: the whole thing, first try.'
      prompt_es: 'Día 2 / pool: el entero, a la primera.'
      starter_code: "int SearchRotated(int[] xs, int want)\n{\n    return -1;\n}\n"
  python:
    fn_name: search_rotated
    sheets:
    - id: r1-01
      rung: 1
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def search_rotated(nums, target):\n    ___\n    while lo <= hi:\n        mid = (lo\
        \ + hi) // 2\n        if nums[mid] == target:\n            return mid\n        if nums[lo] <=\
        \ nums[mid]:\n            if nums[lo] <= target < nums[mid]:\n                hi = mid - 1\n \
        \           else:\n                lo = mid + 1\n        else:\n            if nums[mid] < target\
        \ <= nums[hi]:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n\
        \    return -1\n"
    - id: r1-02
      rung: 1
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def search_rotated(arr, goal):\n    ___\n    while lo <= hi:\n        mid = (lo +\
        \ hi) // 2\n        if arr[mid] == goal:\n            return mid\n        if arr[lo] <= arr[mid]:\n\
        \            if arr[lo] <= goal < arr[mid]:\n                hi = mid - 1\n            else:\n\
        \                lo = mid + 1\n        else:\n            if arr[mid] < goal <= arr[hi]:\n   \
        \             lo = mid + 1\n            else:\n                hi = mid - 1\n    return -1\n"
    - id: r1-03
      rung: 1
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def search_rotated(xs, want):\n    ___\n    while lo <= hi:\n        mid = (lo +\
        \ hi) // 2\n        if xs[mid] == want:\n            return mid\n        if xs[lo] <= xs[mid]:\n\
        \            if xs[lo] <= want < xs[mid]:\n                hi = mid - 1\n            else:\n \
        \               lo = mid + 1\n        else:\n            if xs[mid] < want <= xs[hi]:\n      \
        \          lo = mid + 1\n            else:\n                hi = mid - 1\n    return -1\n"
    - id: r1-04
      rung: 1
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def search_rotated(values, cap):\n    ___\n    while lo <= hi:\n        mid = (lo\
        \ + hi) // 2\n        if values[mid] == cap:\n            return mid\n        if values[lo] <=\
        \ values[mid]:\n            if values[lo] <= cap < values[mid]:\n                hi = mid - 1\n\
        \            else:\n                lo = mid + 1\n        else:\n            if values[mid] <\
        \ cap <= values[hi]:\n                lo = mid + 1\n            else:\n                hi = mid\
        \ - 1\n    return -1\n"
    - id: r1-05
      rung: 1
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def search_rotated(items, limit):\n    ___\n    while lo <= hi:\n        mid = (lo\
        \ + hi) // 2\n        if items[mid] == limit:\n            return mid\n        if items[lo] <=\
        \ items[mid]:\n            if items[lo] <= limit < items[mid]:\n                hi = mid - 1\n\
        \            else:\n                lo = mid + 1\n        else:\n            if items[mid] < limit\
        \ <= items[hi]:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n\
        \    return -1\n"
    - id: r2-01
      rung: 2
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def search_rotated(nums, target):\n    lo, hi = 0, len(nums) - 1\n    ___\n    return\
        \ -1\n"
    - id: r2-02
      rung: 2
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def search_rotated(arr, goal):\n    lo, hi = 0, len(arr) - 1\n    ___\n    return\
        \ -1\n"
    - id: r2-03
      rung: 2
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def search_rotated(xs, want):\n    lo, hi = 0, len(xs) - 1\n    ___\n    return -1\n"
    - id: r2-04
      rung: 2
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def search_rotated(values, cap):\n    lo, hi = 0, len(values) - 1\n    ___\n    return\
        \ -1\n"
    - id: r2-05
      rung: 2
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def search_rotated(items, limit):\n    lo, hi = 0, len(items) - 1\n    ___\n    return\
        \ -1\n"
    - id: r3-01
      rung: 3
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def search_rotated(nums, target):\n    lo, hi = 0, len(nums) - 1\n    while lo <=\
        \ hi:\n        mid = (lo + hi) // 2\n        if nums[mid] == target:\n            ___\n      \
        \  if nums[lo] <= nums[mid]:\n            if nums[lo] <= target < nums[mid]:\n               \
        \ hi = mid - 1\n            else:\n                lo = mid + 1\n        else:\n            if\
        \ nums[mid] < target <= nums[hi]:\n                lo = mid + 1\n            else:\n         \
        \       hi = mid - 1\n    ___\n"
    - id: r3-02
      rung: 3
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def search_rotated(arr, goal):\n    lo, hi = 0, len(arr) - 1\n    while lo <= hi:\n\
        \        mid = (lo + hi) // 2\n        if arr[mid] == goal:\n            ___\n        if arr[lo]\
        \ <= arr[mid]:\n            if arr[lo] <= goal < arr[mid]:\n                hi = mid - 1\n   \
        \         else:\n                lo = mid + 1\n        else:\n            if arr[mid] < goal <=\
        \ arr[hi]:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n  \
        \  ___\n"
    - id: r3-03
      rung: 3
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def search_rotated(xs, want):\n    lo, hi = 0, len(xs) - 1\n    while lo <= hi:\n\
        \        mid = (lo + hi) // 2\n        if xs[mid] == want:\n            ___\n        if xs[lo]\
        \ <= xs[mid]:\n            if xs[lo] <= want < xs[mid]:\n                hi = mid - 1\n      \
        \      else:\n                lo = mid + 1\n        else:\n            if xs[mid] < want <= xs[hi]:\n\
        \                lo = mid + 1\n            else:\n                hi = mid - 1\n    ___\n"
    - id: r3-04
      rung: 3
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def search_rotated(values, cap):\n    lo, hi = 0, len(values) - 1\n    while lo <=\
        \ hi:\n        mid = (lo + hi) // 2\n        if values[mid] == cap:\n            ___\n       \
        \ if values[lo] <= values[mid]:\n            if values[lo] <= cap < values[mid]:\n           \
        \     hi = mid - 1\n            else:\n                lo = mid + 1\n        else:\n         \
        \   if values[mid] < cap <= values[hi]:\n                lo = mid + 1\n            else:\n   \
        \             hi = mid - 1\n    ___\n"
    - id: r3-05
      rung: 3
      kind: rung
      fn_name: search_rotated
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def search_rotated(items, limit):\n    lo, hi = 0, len(items) - 1\n    while lo <=\
        \ hi:\n        mid = (lo + hi) // 2\n        if items[mid] == limit:\n            ___\n      \
        \  if items[lo] <= items[mid]:\n            if items[lo] <= limit < items[mid]:\n            \
        \    hi = mid - 1\n            else:\n                lo = mid + 1\n        else:\n          \
        \  if items[mid] < limit <= items[hi]:\n                lo = mid + 1\n            else:\n    \
        \            hi = mid - 1\n    ___\n"
    - id: full-01
      rung: 4
      kind: full
      fn_name: search_rotated
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "def search_rotated(nums, target):\n    return -1\n"
    - id: full-02
      rung: 4
      kind: full
      fn_name: search_rotated
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "def search_rotated(arr, goal):\n    return -1\n"
    - id: full-recall
      rung: 4
      kind: full
      fn_name: search_rotated
      prompt: 'Day 2 / pool: the whole thing, first try.'
      prompt_es: 'Día 2 / pool: el entero, a la primera.'
      starter_code: "def search_rotated(xs, want):\n    return -1\n"
