id: top-k-frequent
title: Top K Frequent Elements
leetcode_id: lc-top-k-frequent
test_cases:
- args:
  - - 1
    - 1
    - 1
    - 2
    - 2
    - 3
  - 2
  expected:
  - 1
  - 2
- args:
  - - 1
  - 1
  expected:
  - 1
voice:
  prompt: ¿Cuál es la complejidad temporal y espacial?
  prompt_en: Explain the approach and the complexity out loud.
  keywords:
  - o(n)
  - o(1)
  - complejidad
  - enfoque
  - búsquedas
  - conteos
  - trade
  - espacio
  - extra
  - tiempo
languages:
  csharp:
    fn_name: TopKFrequent
    sheets:
    - id: r1-01
      rung: 1
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int[] TopKFrequent(int[] nums, int m)\n{\n    ___\n    foreach (var n in nums)\n\
        \        count[n] = count.GetValueOrDefault(n) + 1;\n    return count.OrderByDescending(kv =>\
        \ kv.Value).Take(m).Select(kv => kv.Key).ToArray();\n}\n"
    - id: r1-02
      rung: 1
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int[] TopKFrequent(int[] arr, int m)\n{\n    ___\n    foreach (var n in arr)\n  \
        \      count[n] = count.GetValueOrDefault(n) + 1;\n    return count.OrderByDescending(kv => kv.Value).Take(m).Select(kv\
        \ => kv.Key).ToArray();\n}\n"
    - id: r1-03
      rung: 1
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int[] TopKFrequent(int[] xs, int k)\n{\n    ___\n    foreach (var n in xs)\n    \
        \    count[n] = count.GetValueOrDefault(n) + 1;\n    return count.OrderByDescending(kv => kv.Value).Take(k).Select(kv\
        \ => kv.Key).ToArray();\n}\n"
    - id: r1-04
      rung: 1
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int[] TopKFrequent(int[] values, int steps)\n{\n    ___\n    foreach (var n in values)\n\
        \        count[n] = count.GetValueOrDefault(n) + 1;\n    return count.OrderByDescending(kv =>\
        \ kv.Value).Take(steps).Select(kv => kv.Key).ToArray();\n}\n"
    - id: r1-05
      rung: 1
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "int[] TopKFrequent(int[] items, int cap)\n{\n    ___\n    foreach (var n in items)\n\
        \        count[n] = count.GetValueOrDefault(n) + 1;\n    return count.OrderByDescending(kv =>\
        \ kv.Value).Take(cap).Select(kv => kv.Key).ToArray();\n}\n"
    - id: r2-01
      rung: 2
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int[] TopKFrequent(int[] nums, int m)\n{\n    var count = new Dictionary<int, int>();\n\
        \    ___\n    return count.OrderByDescending(kv => kv.Value).Take(m).Select(kv => kv.Key).ToArray();\n\
        }\n"
    - id: r2-02
      rung: 2
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int[] TopKFrequent(int[] arr, int m)\n{\n    var count = new Dictionary<int, int>();\n\
        \    ___\n    return count.OrderByDescending(kv => kv.Value).Take(m).Select(kv => kv.Key).ToArray();\n\
        }\n"
    - id: r2-03
      rung: 2
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int[] TopKFrequent(int[] xs, int k)\n{\n    var count = new Dictionary<int, int>();\n\
        \    ___\n    return count.OrderByDescending(kv => kv.Value).Take(k).Select(kv => kv.Key).ToArray();\n\
        }\n"
    - id: r2-04
      rung: 2
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int[] TopKFrequent(int[] values, int steps)\n{\n    var count = new Dictionary<int,\
        \ int>();\n    ___\n    return count.OrderByDescending(kv => kv.Value).Take(steps).Select(kv =>\
        \ kv.Key).ToArray();\n}\n"
    - id: r2-05
      rung: 2
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "int[] TopKFrequent(int[] items, int cap)\n{\n    var count = new Dictionary<int,\
        \ int>();\n    ___\n    return count.OrderByDescending(kv => kv.Value).Take(cap).Select(kv =>\
        \ kv.Key).ToArray();\n}\n"
    - id: r3-01
      rung: 3
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int[] TopKFrequent(int[] nums, int m)\n{\n    var count = new Dictionary<int, int>();\n\
        \    foreach (var n in nums)\n        count[n] = count.GetValueOrDefault(n) + 1;\n    ___\n}\n"
    - id: r3-02
      rung: 3
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int[] TopKFrequent(int[] arr, int m)\n{\n    var count = new Dictionary<int, int>();\n\
        \    foreach (var n in arr)\n        count[n] = count.GetValueOrDefault(n) + 1;\n    ___\n}\n"
    - id: r3-03
      rung: 3
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int[] TopKFrequent(int[] xs, int k)\n{\n    var count = new Dictionary<int, int>();\n\
        \    foreach (var n in xs)\n        count[n] = count.GetValueOrDefault(n) + 1;\n    ___\n}\n"
    - id: r3-04
      rung: 3
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int[] TopKFrequent(int[] values, int steps)\n{\n    var count = new Dictionary<int,\
        \ int>();\n    foreach (var n in values)\n        count[n] = count.GetValueOrDefault(n) + 1;\n\
        \    ___\n}\n"
    - id: r3-05
      rung: 3
      kind: rung
      fn_name: TopKFrequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "int[] TopKFrequent(int[] items, int cap)\n{\n    var count = new Dictionary<int,\
        \ int>();\n    foreach (var n in items)\n        count[n] = count.GetValueOrDefault(n) + 1;\n\
        \    ___\n}\n"
    - id: full-01
      rung: 4
      kind: full
      fn_name: TopKFrequent
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "int[] TopKFrequent(int[] nums, int m)\n{\n    return new int[] {};\n}\n"
    - id: full-02
      rung: 4
      kind: full
      fn_name: TopKFrequent
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "int[] TopKFrequent(int[] arr, int m)\n{\n    return new int[] {};\n}\n"
    - id: full-recall
      rung: 4
      kind: full
      fn_name: TopKFrequent
      prompt: 'Day 2 / pool: the whole thing, first try.'
      prompt_es: 'Día 2 / pool: el entero, a la primera.'
      starter_code: "int[] TopKFrequent(int[] xs, int k)\n{\n    return new int[] {};\n}\n"
  python:
    fn_name: top_k_frequent
    sheets:
    - id: r1-01
      rung: 1
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def top_k_frequent(nums, n):\n    ___\n    return [x for x, _ in Counter(nums).most_common(n)]\n"
    - id: r1-02
      rung: 1
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def top_k_frequent(arr, m):\n    ___\n    return [x for x, _ in Counter(arr).most_common(m)]\n"
    - id: r1-03
      rung: 1
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def top_k_frequent(xs, k):\n    ___\n    return [x for x, _ in Counter(xs).most_common(k)]\n"
    - id: r1-04
      rung: 1
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def top_k_frequent(values, steps):\n    ___\n    return [x for x, _ in Counter(values).most_common(steps)]\n"
    - id: r1-05
      rung: 1
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 1: set up the start (structures / first line). Different variable names.'
      prompt_es: 'Peldaño 1: arma el inicio (estructuras / primera línea). Otras variables.'
      starter_code: "def top_k_frequent(items, cap):\n    ___\n    return [x for x, _ in Counter(items).most_common(cap)]\n"
    - id: r2-01
      rung: 2
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def top_k_frequent(nums, n):\n    from collections import Counter\n    ___\n"
    - id: r2-02
      rung: 2
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def top_k_frequent(arr, m):\n    from collections import Counter\n    ___\n"
    - id: r2-03
      rung: 2
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def top_k_frequent(xs, k):\n    from collections import Counter\n    ___\n"
    - id: r2-04
      rung: 2
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def top_k_frequent(values, steps):\n    from collections import Counter\n    ___\n"
    - id: r2-05
      rung: 2
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 2: the loop / the middle idea.'
      prompt_es: 'Peldaño 2: el recorrido / la idea del medio.'
      starter_code: "def top_k_frequent(items, cap):\n    from collections import Counter\n    ___\n"
    - id: r3-01
      rung: 3
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def top_k_frequent(nums, n):\n    from collections import Counter\n    ___\n"
    - id: r3-02
      rung: 3
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def top_k_frequent(arr, m):\n    from collections import Counter\n    ___\n"
    - id: r3-03
      rung: 3
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def top_k_frequent(xs, k):\n    from collections import Counter\n    ___\n"
    - id: r3-04
      rung: 3
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def top_k_frequent(values, steps):\n    from collections import Counter\n    ___\n"
    - id: r3-05
      rung: 3
      kind: rung
      fn_name: top_k_frequent
      prompt: 'Rung 3: the returns and the close.'
      prompt_es: 'Peldaño 3: los return y el cierre.'
      starter_code: "def top_k_frequent(items, cap):\n    from collections import Counter\n    ___\n"
    - id: full-01
      rung: 4
      kind: full
      fn_name: top_k_frequent
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "def top_k_frequent(nums, n):\n    return []\n"
    - id: full-02
      rung: 4
      kind: full
      fn_name: top_k_frequent
      prompt: Full algorithm, first try.
      prompt_es: Algoritmo completo, a la primera.
      starter_code: "def top_k_frequent(arr, m):\n    return []\n"
    - id: full-recall
      rung: 4
      kind: full
      fn_name: top_k_frequent
      prompt: 'Day 2 / pool: the whole thing, first try.'
      prompt_es: 'Día 2 / pool: el entero, a la primera.'
      starter_code: "def top_k_frequent(xs, k):\n    return []\n"
